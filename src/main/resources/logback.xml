<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- LUMIDISK_LOG_DIR は MainApp.configureLogDirectory() で OS 別に設定される。
       未設定の場合 (CLI 経由のテスト等) は ./logs にフォールバック。 -->
  <property name="LOG_DIR" value="${LUMIDISK_LOG_DIR:-logs}" />

  <!-- コンソール出力 -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ファイル出力 -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/lumidisk.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/lumidisk.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>7</maxHistory>
      <totalSizeCap>50MB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ルートロガー -->
  <root level="INFO">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="FILE" />
  </root>

  <!-- アプリケーション固有のログレベル -->
  <logger name="com.example.diskanalyzer" level="DEBUG" />
</configuration>
